# PostgreSQL silver store (live mode)
DATABASE_URL=postgresql+psycopg2://nba:nba@localhost:5432/nba_warehouse

# Smoke mode uses SQLite + local DuckDB by default; no env needed.
# SMOKE_DATABASE_URL=sqlite:///data/smoke/silver.db

# NBA Stats API politeness
NBA_API_TIMEOUT_SECONDS=30
